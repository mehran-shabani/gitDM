# Django Settings Mode
# Options: 'simple' or 'full'
SETTINGS_MODE=simple

# Django Core Settings
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Settings (for full mode)
POSTGRES_DB=diabetes
POSTGRES_USER=diabetes
POSTGRES_PASSWORD=diabetes
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# Redis Settings (for full mode)
REDIS_URL=redis://localhost:6379/0

# MinIO Settings (for full mode)
MINIO_STORAGE_ENDPOINT=localhost:9000
MINIO_STORAGE_ACCESS_KEY=minioadmin
MINIO_STORAGE_SECRET_KEY=minioadmin
MINIO_STORAGE_USE_HTTPS=False
MINIO_STORAGE_MEDIA_BUCKET=media
MINIO_STORAGE_STATIC_BUCKET=static

# Docker Compose Mode
# Options: 'simple' or 'full'
COMPOSE_MODE=simple