# Django settings
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=your-secret-key-here-change-this-in-production

# Database settings (SQLite for development)
DATABASE_URL=sqlite:///db.sqlite3

# Optional: Set to True when running in GitHub Codespaces
CODESPACES=False

# Optional: Set timezone
TIME_ZONE=UTC

# Optional: Set language code
LANGUAGE_CODE=en-us

# Optional: Email settings for development
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Optional: Media and static files
MEDIA_ROOT=media/
STATIC_ROOT=staticfiles/

# Optional: Allowed hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1,*.gitpod.io,*.codespaces.githubusercontent.com

# Optional: CORS settings
CORS_ALLOW_ALL_ORIGINS=True

# Optional: API settings
API_TITLE=Medical System API
API_VERSION=1.0.0
API_DESCRIPTION=A comprehensive medical system API